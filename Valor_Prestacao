valorCompra = float(input('Digite o valor da compra: '))
prestacao = int(input('Digite o numero de prestacoes: '))
valorPrestacao = valorCompra / prestacao

print('O valor da prestacao e:', valorPrestacao)
